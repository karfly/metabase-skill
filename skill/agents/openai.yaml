interface:
  display_name: "Metabase API"
  short_description: "Explore schema, search saved content, and run Metabase queries through the bundled self-contained API client."
  default_prompt: "Use this skill to work with Metabase through the bundled metabase client inside the installed skill folder: validate access, inspect databases and tables, search saved content, and run saved or ad-hoc queries without MCP."
